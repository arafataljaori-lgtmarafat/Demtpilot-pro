# سجل التغييرات — DentPilot

## v2.6.1 — نظام تفعيل DP3 الموحّد (متزامن مع Admin وStudent)
- إضافة دعم أكواد **DP3** (`DP3-P-<أيام Base36>-<Nonce>-<توقيع>`) — نفس مواصفة Student تماماً، بمعرّف تطبيق مستقل (يرفض أكواد Student والعكس).
- يدعم أربع خطط: شهري، ستة أشهر، سنوي، دائم — المدة تبدأ من لحظة التفعيل الفعلي داخل التطبيق لا لحظة الإصدار.
- منع إعادة استخدام نفس الكود لتمديد المدة، ومقاومة تراجع ساعة الجهاز للخلف.
- إضافة `getActivationInfo` إلى `window.DPLicense` (لم تكن موجودة سابقاً)، تُستخدم الآن في صفحة الدعم والإعدادات لعرض اسم الخطة الحقيقي وتاريخ الانتهاء بدلاً من رسالة "مفعّل" العامة دائماً — لا تُعرض خطة شهرية أو سنوية كأنها دائمة بعد الآن.
- مصدر الحقيقة هو حالة التفعيل الموقّعة داخل `dentpilot_license` نفسه؛ لا اعتماد على `dentpilot_license_meta` (بقي كما كان: مفتاح قراءة اختياري غير مُنشأ من هذا الملف).
- **التوافق الخلفي كامل**: أكواد legacy تبقى مقبولة كتفعيل دائم دون أي تغيير.
- **قيد معماري صريح**: نفس قيد Student — التفعيل محلي بالكامل، ومنع إعادة الاستخدام مضمون ضمن الاستخدام العادي فقط.
- لم تتغيّر: بيانات العيادة والمرضى، Firebase Auth والمزامنة، التجربة المجانية 72 ساعة.

## v2.6.0 (2026) — الحسابات والمزامنة السحابية

- إضافة الحساب بالبريد الإلكتروني وكلمة المرور.
- إضافة تسجيل الدخول والخروج واستعادة كلمة المرور.
- إضافة مزامنة بيانات المرضى والجلسات والإعدادات.
- استعادة بيانات العيادة عند تغيير الجهاز.
- تطوير صفحة الدعم والتفعيل وإضافة الوكيل.
- الصور والمرفقات غير مشمولة بالمزامنة حالياً.

## v2.5.1 (2026) — جولة Refinement للمرحلة الأولى (بصري فقط)

- **Hero:** توازن أدق بين النص والسن (58/42)، خط هوية فيروزي رفيع تحت الاسم، شارة DentPilot العلوية أهدأ.
- **السن:** إعادة رسم — تفاصيل Enamel أدق (فصوص عمودية وحواف إطباقية)، جذران أطبع بتفرّع واضح، تظليل جانبي وعنقي للعمق، إضاءة Specular مخفَّفة، وذوبان تدريجي للجذور في كحلي الخلفية بدل الهالة الدائرية.
- **شارة التجربة:** أسلوب زجاجي هادئ (Blur + حدود فيروزية) واضح دون سيطرة بصرية — نفس العنصر والمنطق.
- **الدرج:** أيقونات بلا صناديق، تمييز العنصر النشط وحده بخلفية وشريط جانبي (بصري فقط عبر المسار الحالي)، وفاصلا مجموعات بسيطان — نفس الروابط والوظائف.
- **الرئيسية:** إيقاع مسافات أهدأ وظلال أخف لطابع تطبيق طبي يومي.

## v2.5.0 (2026) — Premium Visual Foundation (المرحلة الأولى: الصفحة الرئيسية)

تحديث بصري وتجربة استخدام فقط — **بلا أي تغيير في LocalStorage أو مفاتيح البيانات أو منطق المرضى/الجلسات أو التفعيل أو Device Lock أو النسخ الاحتياطي**.

- **نظام تصميم موحّد (dp-\*):** رموز CSS للألوان (Midnight Navy / Deep Medical Blue / Teal / Cyan Accent محدود)، والمسافات، والزوايا، والظلال، والحدود، وحالات الأزرار، والشارات — Mobile First وRTL من 360px إلى 430px بلا Horizontal Overflow.
- **App Bar جديد:** زر قائمة + هوية DentPilot + اسم الطبيب والتخصص + الإشعارات، بتصميم بسيط ومرتفع الجودة.
- **Hero Card مُعاد بناؤه:** كرت أفقي بتدرج Navy→Teal مع عمق وإضاءة داخلية وتفاصيل طبية رقمية خفيفة، يضم اسم الطبيب/العيادة والوصف، مع بقاء شارة التجربة داخله قابلة للنقر وبنفس منطقها.
- **Dental Tooth Artwork جديد:** سن Premium Semi-3D بـ SVG داخلي (تاج وجذور وتدرجات Enamel وHighlight وShadow وهالة رقمية خفيفة) كجزء من تكوين الـ Hero.
- **قائمة جانبية (Side Drawer) من اليمين:** خلفية Midnight Navy بتدرج وشبكة رقمية خفيفة، Overlay مع Blur، فتح/إغلاق ناعم، إغلاق بالنقر خارجها وبـ Escape، وقفل تمرير الخلفية. تضم الوظائف الفعلية فقط: الرئيسية، المرضى، التقارير، النسخة الاحتياطية، الدعم والتفعيل، الإعدادات.
- **إعادة ترتيب الرئيسية:** إحصائيات مضغوطة أنيقة (المرضى / مواعيد اليوم / المتأخرة) بأيقونات Stroke موحّدة، و«إضافة مريض جديد» كإجراء أساسي بارز، مع نقل التقارير/النسخة الاحتياطية/الدعم إلى القائمة الجانبية دون حذفها.
- **Bottom Navigation** للأقسام الأساسية (الرئيسية / المرضى / الإعدادات) يظهر في الصفحة الرئيسية.
- **مواعيد اليوم:** بطاقات مضغوطة (اسم/وقت/حالة) وEmpty State صغير بأيقونة SVG — بلا تغيير في منطق المواعيد.
- **أيقونات:** استبدال أي Emoji في الصفحة الرئيسية بأيقونات SVG Stroke موحّدة السماكة، بلا مكتبات خارجية.
- **حركة خفيفة فقط:** انتقالات القائمة، تأثير ضغط الأزرار، ظهور هادئ — بلا Bounce أو Glow متحرك، مع احترام تفضيل تقليل الحركة.
- **PWA:** رفع الإصدار إلى 2.5.0 (version.json + كاش `dentpilot-v2.5.0`) لتسليم الواجهة الجديدة للأجهزة المثبَّتة بأمان.

> نطاق هذه المرحلة الصفحة الرئيسية فقط؛ الشاشات الداخلية (إضافة المريض، قائمة المرضى، ملف المريض، الجلسات، الدفعات، المرفقات) لم تُمَسّ وستُحدَّث في مراحل مستقلة لاحقة.

## v2.0.0 — Release Candidate (2026)

مرشّح إصدار مستقر وجاهز للاستخدام اليومي في العيادات.

### تحسينات هذا الإصدار (الاستقرار والتلميع)
- **مراجعة شاملة للكود:** إزالة أي منطق مكرّر أو ميت، وتوحيد الدوال. لا توجد دوال JavaScript أو فئات CSS غير مستخدمة.
- **إزالة المحتوى التجريبي:** لم يعد هناك أي اسم طبيب ثابت؛ حقل الاسم يعرض «مثال: د. اسم الطبيب»، والتطبيق عام لأي طبيب أسنان.
- **أداء:** إضافة تهدئة (debounce) لحقل البحث لتقليل إعادة الرسم، وتقليل العمليات الحسابية المكرّرة.
- **إمكانية الوصول:** إبراز واضح للعناصر عند التنقّل بلوحة المفاتيح (`:focus-visible`).
- **PWA:** تحديث نسخة الكاش إلى `dentpilot-v2.1.0-rc` لضمان جلب أحدث الملفات.
- **تدقيق وظيفي كامل:** تم اختبار كل المسارات آلياً (لوحة التحكم، الإضافة/التعديل، الملف، الجلسات، الدفعات، الخط الزمني، المرفقات، الإحصائيات، التقارير، النسخ الاحتياطي/الاستعادة، الأرشفة/الاستعادة، الطباعة، البحث، التنقّل، الإعدادات) — دون أخطاء في وحدة التحكم.

### الميزات المكتملة في v2.0
- لوحة تحكم ببطاقات فورية (مرضى، مواعيد اليوم، متأخرة، إحصائيات، تقارير، نسخة احتياطية، إعدادات، إضافة).
- إعداد أول تشغيل لبيانات الطبيب/العيادة، وتخصيص الاسم في كل التطبيق.
- إدارة المرضى: إضافة/تعديل/حذف + بطاقات مدمجة + بحث بالاسم/الهاتف/العلاج.
- ملف المريض: معلومات + **جلسات علاج** (غير محدودة، رقم تلقائي، تاريخ ويوم الأسبوع، وقت، علاج، مبلغ مدفوع، ملاحظات، موعد قادم، خانة إنجاز) + **خط زمني رأسي** + **سجل دفعات تلقائي** + **مرفقات** (صور/مستندات).
- نظام مالي تلقائي: التكلفة/المدفوع/المتبقي تتحدّث فوراً مع كل جلسة.
- إنهاء العلاج → **أرشفة** في المرضى المكتملين (دون حذف)، مع **استعادة** و**حذف نهائي**.
- **طباعة** تقرير A4 احترافي (معلومات، خط زمني، جلسات، دفعات، إجماليات، تاريخ الإكمال، الطبيب، العيادة، الشعار).
- تنبيهات المواعيد (اليوم/الغد) صوتية وبصرية، اتصال وواتساب مباشرين.
- نسخ احتياطي/استعادة (JSON)، إحصائيات، تقارير.
- PWA: قابل للتثبيت، يعمل دون إنترنت، متجاوب (Android/iPhone/Tablet)، RTL كامل.

### توافق البيانات
جميع بيانات المرضى تُحفظ بنفس مفتاح `LocalStorage` المستخدم منذ الإصدارات الأولى
(`qarabesh_clinic_patients_v1`)، والحقول الجديدة (الجلسات، حالة الإكمال) تُضاف بتوافق رجعي.
المرفقات تُخزَّن بمفتاح منفصل، والإعدادات بمفتاح ثالث. الاستيراد/التصدير يشمل الجميع.

### حدود المتصفح المعروفة
- **المرفقات** تُخزَّن محلياً (base64) داخل `LocalStorage`؛ سعة المتصفح محدودة (عادة ~5 ميغابايت).
  يجري تصغير الصور تلقائياً، وعند امتلاء المساحة تظهر رسالة واضحة. للأرشفة الكثيفة يُنصح بتصدير نسخة احتياطية دورياً.
- **الطباعة** تعتمد على مربّع طباعة المتصفح (لا تصدير PDF مباشر)؛ اختر «حفظ كـ PDF» من نافذة الطباعة عند الحاجة.
- **ميزات PWA** (التثبيت والعمل دون إنترنت) تتطلّب تقديم التطبيق عبر رابط HTTPS (مثل Netlify)، لا فتحه بصيغة `file://`.
- **التنبيه الصوتي** قد يتطلّب تفاعل المستخدم أول مرة (سياسة تشغيل الصوت في المتصفحات).

### إضافة: نظام تفعيل محلي (RC2)
- بوابة تفعيل لمرة واحدة على الجهاز: يعرض التطبيق «معرّف الجهاز»، ويُفعَّل بكود مرتبط بذلك المعرّف فقط.
- الخوارزمية (SHA-256 + مفتاح سري مُعتَّم + جولات اشتقاق) متطابقة حرفياً بين التطبيق وأداة `license-generator.html`.
- تعمل بالكامل دون إنترنت/خادم. أداة المولّد **منفصلة** ولا تُسلَّم للعميل.
- حدّ معروف: أي حماية من طرف العميل قابلة للكسر نظرياً (المفتاح موجود في ملفات التطبيق)؛ التعتيم يرفع الحاجز فقط. للحماية القوية يلزم تحقّق عبر خادم.
